<ul id="lists" class="clearfix">
    {{#each lists}}
    <a href="#/dashboard/{{../Id}}/list/{{Id}}">
        <li class="singleList">
            <h3>
                {{title}}
            </h3>
            <p class="listDescription">{{description}}</p>
        </li>
        </a>
    {{/each}}
</ul>
<a class="addItemBtn btn" data-toggle="modal" data-target="#addListModal">+ Add List</a>